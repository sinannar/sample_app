<li>
	<%= link_to user.name ,user %>
	<%= user.admin %>
	<% if current_user.admin?  %>
	| 
	<%= link_to "Delete", 
				user, 
				:method => :delete,
				:confirm => 'Are u sure ?' ,
		 		:title => "Delete #{user.name}" %>
	<% end %>
</li>