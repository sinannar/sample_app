<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class = "field">
		url:</br>
		<%= f.text_area :content %></br>
		tags:</br>
		<%= f.text_area :tag %></br>
	</div>
	<div class = "actions">
		<%= f.submit "submit" %>
	</div>
<% end %>